# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MovieApp' do
  use_framework!
  
  pod 'Alamofire'
  pod 'Kingfisher', '~> 7.0'
  pod 'SideMenu'
end
